{
  "name": "tab-share-shortener",
  "version": "0.2.2",
  "description": "First-party link shortener for Tab Share -- accepts share links of any length, two code modes (random / readable words), no account, self-hostable.",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node scripts/dev.mjs",
    "gen:worker": "node scripts/gen-worker.mjs",
    "test": "node scripts/selftest-store.mjs && node scripts/selftest.mjs && node scripts/selftest-worker.mjs"
  },
  "devDependencies": {}
}
